[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "academicsubmission"
version = "0.1.0"
description = "Minimal evidence-seeking agent training scaffold"
requires-python = ">=3.8"
